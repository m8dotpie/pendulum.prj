package api;

struct command {
    int8_t code;
    string msg;
}

struct response {
    int16_t p_distance;
    int16_t p_angle;
    int8_t m_angle;
    int16_t m_torque;
    int16_t m_speed;
    int16_t m_position;
}